name: xtalk_4th_lt
description: "X Talk #4 用の LT 資料"
publish_to: 'none'

version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_deck: ^0.22.0
  flutter_localization: ^0.3.3
  flutter_screenutil: ^5.9.3
  gap: ^3.0.1
  intl: ^0.20.2
  timelines_plus: ^1.0.7
  tutorial_coach_mark: ^1.3.3
  url_launcher: ^6.3.1
  animated_text_kit: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/images/